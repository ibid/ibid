processors = core.Addressed, irc.Actions, core.Ignore, admin.ListModules, admin.LoadModules, basic.Greet, info.DateTime, basic.SayDo, test.Delay, seen.Seen, sources.Connect, sources.Disconnect, newarch.NewModuleTest1, basic.Complain, core.Responses, seen.Watch, log.Log
botname = Ibid
[sources]
    [[atrum]]
        channels = "#ibid",
        nick = $botname
        type = irc
        server = za.atrum.org
    [[jabber]]
        ssl = True
        jid = ibidbot@jabber.org/source
        password = ibiddev
        type = jabber
        server = jabber.org
    [[telnet]]
        type = telnet
        port = 3000
    [[clock]]
        step = 5
        type = timer
[modules]
    [[core.Ignore]]
        ignore = NickServ,
    [[ping]]
        bus_name = org.ibid.module.Ping
        pattern = ^ping$
        type = dbus.Proxy
        object_path = /org/ibid/module/Ping
    [[log.Log]]
        logfile = /tmp/ibid.log
    [[core.Addressed]]
        names = $botname, bot, Ibid, CIbid, VIbid, jibid
[databases]
	[[ibid]]
		uri = sqlite:///ibid.db
