load = core, basic, info, log, http, irc, seen, identity, admin, auth, help, test, sources, config, bzr, network, factoid, roshambo, python, crypto, tools, morse, dict
botname = Ibid

[auth]
	methods = password,
	timeout = 300
	permissions = +factoid,

[sources]
    [[atrum]]
        channels = "#ibid",
        nick = $botname
        type = irc
        server = za.atrum.org
		auth = hostmask, nickserv,
		mode = B
		permissions = +karma,
	[[freenode]]
		channels = 
		nick = $botname
		type = irc
		server = chat.eu.freenode.net
		auth = hostmask, nickserv
		disabled = True
    [[jabber]]
        ssl = True
        jid = ibidbot@jabber.org/source
        password = ibiddev
        type = jabber
        server = jabber.org
		auth = implicit,
    [[telnet]]
        type = telnet
        port = 3001
    [[manhole]]
        type = manhole
        port = 9898
    [[clock]]
        step = 5
        type = timer
	[[http]]
		type = http
	[[email]]
		type= smtp
		relayhost = localhost
		address = ibid@localhost

[plugins]
    [[ping]]
        bus_name = org.ibid.module.Ping
        pattern = ^ping$
        type = dbus.Proxy
        object_path = /org/ibid/module/Ping
    [[core]]
        names = $botname, bot, ant
        ignore = NickServ,
	[[bzr]]
		repository = http://ibid.omnia.za.net/
		source = atrum
		channel = "#ibid"

[databases]
	[[ibid]]
		uri = sqlite:///ibid.db
